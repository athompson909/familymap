<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.bignerdranch.android.familymap3"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Family Map"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="base.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="base.PersonActivity"/>
        <activity android:name="base.MapActivity"/>
        <activity android:name="base.SettingsActivity"/>
        <activity android:name="base.FilterActivity"/>
        <activity android:name="base.SearchActivity"/>
    </application>

</manifest>
